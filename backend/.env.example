DATABASE_URL="postgresql://postgres:postgres@localhost:5432/qrpay?schema=public"
PORT=4000
FRONTEND_URL="http://localhost:5173"
# Optional. Open Food Facts asks for a descriptive User-Agent for barcode lookups.
# OPENFOODFACTS_USER_AGENT="YourStore/1.0 (contact@yourdomain.com)"
RESEND_API_KEY=re_gzHRAcJw_4m67XexhW8gvrErWhjdnHs8W
RESEND_FROM_EMAIL="EasyPay <noreply@example.com>"
PLATFORM_URL="http://localhost:5432"



# Perfect work so far. Now what am thinking, if we have our product, can we scan the Barcode on the product and it look up from open food  fact without manually entering the code and if the product is not then we manually input the details and add an image for it. so that at the POS/Checkout which is currently in simulation mode can be change to pick up real scans. Is this possible like using the cam/mobile as we want to next convert to app so that this procedure to be done using mobile.